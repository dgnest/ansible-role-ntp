---
# vars file for hadenlabs.ntp
_ntp_file_path: '/etc/ntp.conf'

_ntp:
  templates:
    - src: "ntp.conf.j2"
      dest: "{{ _ntp_file_path}}"

# _ntp.create_files:
#   - path: "{{ _path_dir }}"
#     state: "directory"
#     # group: "root"
#     owner: "{{ user }}"
